# Example Skill (Plugin: _example)

This skill would be loaded by Hermes when the plugin is active.
It demonstrates how plugins can ship reusable procedural knowledge.

## Example Command

```bash
echo "Hello from example plugin skill!"
```
