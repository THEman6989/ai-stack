# Plugin: _example

Referenz-Plugin für das AlphaRavis Plugin-System. Standardmäßig deaktiviert.

## Aktivierung

`.pluginenv` editieren: `ENABLED=false` → `ENABLED=true`

## Eigene Plugins bauen

Siehe `docs/ALPHARAVIS_PLUGIN_SYSTEM.md`
